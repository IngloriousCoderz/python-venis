tuple = 1, 2, 3
tuple # prints (1, 2, 3)
tuple[1] # prints 2

tuple = (1, 2, 3) # parentheses make tuple more clear
empty_tuple = () # an empty tuple requires parentheses

message = 'hello', # a comma creates a tuple!
message # prints ('hello',)

# tuples are immutable, there's not methods to change them
